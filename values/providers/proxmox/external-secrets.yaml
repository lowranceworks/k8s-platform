# Proxmox-specific External Secrets Operator values.
#
# On-premises clusters connecting to GCP Secret Manager
# will need a GCP service account key or workload identity
# configured via a ClusterSecretStore (managed separately).

external-secrets: {}
