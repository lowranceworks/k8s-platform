apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dev-proxmox-home-tls
  namespace: argocd
  labels:
    lifecycle: dev
    provider: proxmox
    region: home
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: dev-proxmox-home
    creationPolicy: Merge
  data:
    - secretKey: config
      remoteRef:
        key: argocd-cluster-dev-proxmox-home
